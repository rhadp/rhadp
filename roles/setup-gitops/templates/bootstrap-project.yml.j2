kind: AppProject
apiVersion: argoproj.io/v1alpha1
metadata:
  name: "{{ rhadp_prefix }}-bootstrap"
  namespace: "openshift-gitops"
spec:
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
  destinations:
    - namespace: "{{ rhadp_prefix }}-infra"
      server: "*"
  sourceRepos:
    - "*"
